

const PostCard = ({author, post}) => {
  return (
    <div>
    what's going on?
    
      <p style={{ maxWidth: "400px" }}>{post} </p>
      <p>{author}</p>
    </div>
  )
}

export default PostCard